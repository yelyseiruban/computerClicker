package com.example.clicker.model.game

class ItemCharacteristics (val startPrice: Long, val pointsPerSecond: Int)

enum class ItemName {
    MOUSE,
    KEYBOARD,
    PROGRAMMER,
    FRIENDS,
    TEAM,
    COMPANY
}
